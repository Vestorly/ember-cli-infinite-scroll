## Customize the test machine
machine:
  # Version of node to use
  node:
    version: 8.9.1

test:
  override:
    - npm test:
        timeout: 900

## Customize dependencies
dependencies:
  cache_directories:
    - "~/.cache/yarn"
  pre:
    - npm install -g yarn
  override:
    - yarn
